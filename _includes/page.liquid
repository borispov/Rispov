---
layout: default
---

<div class="container mx-auto my-12">
  <section class="w-full flex flex-wrap py-6 justify-between">
    <main class="w-full md:w-2/3 flex flex-col items-center bg-red-100 rounded">
        {% if format == 'md' %}
          <div class="markdown p-4">
            {{ content }}
          </div>
        {% else %}
          {{ content }}
        {% endif %}
    </main>
    {% include sidebar %}
  </section>
</div>
